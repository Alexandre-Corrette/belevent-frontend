/**
 * Mercure SSE plugin — client-side only.
 * Makes useMercure composable available across the app.
 */
export default defineNuxtPlugin(() => {
  // The composable is auto-imported by Nuxt.
  // This plugin ensures it's only loaded client-side.
})